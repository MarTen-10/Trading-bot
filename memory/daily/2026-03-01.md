
## 2026-03-01 — Phase 1 Trading System Scaffolding (Task B + C)
- Attempted: scaffold multi-agent structure and core repo contracts for Anubis/Thoth/Amun/Ma’at/Horus.
- Changed:
  - Added `agents/` with per-agent `SOUL.md` + `AGENTS.md` role/boundary contracts.
  - Added `core/` module layout (`data`, `features`, `backtest`, `risk`, `execution`) + invariant docs.
  - Added `core/contracts/risk_ticket.schema.json` enforcing hard Ma’at ticket shape.
  - Added `configs/{strategy,risk,exchange}.yaml` for 30m breakout v1, hard risk limits, Coinbase API surface selection.
- Outcome: phase-1 structure and deterministic inter-agent packet schema in place; ready for module implementation.
- Reusable lesson: enforce `RiskTicket` as explicit schema artifact early to prevent execution bypass later.
- Follow-ups:
  - Implement DB migrations + persistence contracts.
  - Implement Coinbase adapter with Advanced Trade perps first.
  - Wire Horus hard check: reject order if RiskTicket invalid/missing.

## Task: Infra hardening phase 1 (DB/risk/execution paper)
- Attempted: Implemented v1 DB migration + Ma’at hard gate + Coinbase adapter skeleton + paper execution engine (+ data feed skeleton).
- Changed: Added core/db/migrations/0001_infra_foundation.sql, core/db/postgres.py, core/db/migrate.py, core/risk/validator.py, core/execution/{coinbase_adapter,paper_engine,router}.py, core/data/market_data_feed.py.
- Outcome: Modules compile; risk gate DENY/ALLOW path verified; adapter returns safe missing-keys response for balance fetch.
- Reusable lesson: Enforce risk-ticket invariants at both DB (NOT NULL hash) and router/validator layers.
- Follow-ups: Wire Coinbase request signing, add integration tests against local Postgres, integrate validator into Horus runtime order path.

## Task: tighten-ups + Coinbase auth/read-only harness
- Attempted: Applied DB constraint tighten-ups, router exactly-once enforcement, secrets provider, Coinbase signing module, readonly smoketest harness, and paper forward loop harness.
- Changed: Added migration 0002, updated validator/router/postgres adapter, added coinbase_signing + unit tests, added scripts/coinbase_readonly_smoketest.py and scripts/paper_forward_run.py.
- Outcome: Duplicate trade_id is blocked with reconciliation log; signature format fixed-length enforcement added; smoketest correctly classifies auth failures without keys.
- Reusable lesson: classify non-JSON 401/403 as auth_error, not parse_error, to avoid silent auth drift.
- Follow-ups: run migrations on live Postgres, run smoketest again after key install, run 24h unattended paper service with persisted metrics/logs.
